# Cloudinary — ключ для заливки фото дневника (npm run upload-photos).
# Скопируй этот файл в .env и подставь своё значение.
# Взять на cloudinary.com → Dashboard → "API environment variable" (формат ниже).
CLOUDINARY_URL=cloudinary://API_KEY:API_SECRET@CLOUD_NAME
