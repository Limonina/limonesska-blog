# Cloudinary — заливка фото дневника (см. scripts/upload-photos.mjs).
# Скопируй в .env и впиши строку из Cloudinary → Dashboard → API Environment variable.
# Нужен только локально для `npm run upload-photos`; на сборку/деплой не влияет.
CLOUDINARY_URL=cloudinary://API_KEY:API_SECRET@CLOUD_NAME
